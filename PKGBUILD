# Maintainer: Patrick Motard <motard19@gmail.com>

pkgname=python-polybar-reload
pkgver=0.1.0
pkgrel=1
pkgdesc='Reloads polybar when screen resolution has changed'
arch=('any')
license=('MIT')
depends=('python')
makedepends=('python-setuptools')

build() {
    python setup.py build        
}

package() {
  
